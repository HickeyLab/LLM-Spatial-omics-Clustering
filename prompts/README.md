# LLM prompts

Place future version-controlled prompt templates, output schemas, and cell-type
label ontologies here.

When implementation begins, record the model identifier, model version,
parameters, prompt version or hash, and raw response provenance for every LLM
run used in a manuscript figure. Do not add API keys or credentials.
